import React from 'react';
import PropTypes from 'prop-types';


const ProgressBar = ({progress, striped, animated}) => {
    return <div>ProgressBar</div>
};

ProgressBar.propTypes = {
    progress: PropTypes.progress,
    
};

export default ProgressBar;
